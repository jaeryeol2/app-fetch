#!/bin/sh
# $3 is 1 for a branch checkout and 0 for a file checkout; only the former can
# move enough code to invalidate the graph.
[ "$3" = "1" ] || exit 0
command -v graft >/dev/null 2>&1 && graft build >/dev/null 2>&1
exit 0
