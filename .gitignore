# Dependencies
node_modules/

# CodeGraph local index
.codegraph/

# Build Outputs
dist/

# Logs & Debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage / Testing Artifacts
coverage/
.nyc_output/
tests/ 

# IDE & Tooling
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# OS Metadata
.DS_Store
Thumbs.db

# Environment & Temp
.env
.env.local
.env.*.local
.gemini/
tmp/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# graft's agent wiring — machine-specific (an absolute path is baked into the
# helpers), regenerable per developer with `graft init`.
.claude/helpers/
.claude/settings.json
